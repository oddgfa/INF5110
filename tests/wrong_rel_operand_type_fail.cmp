program wrong_rel_operand_type_fail
begin

    // Arguments of a relational operator not of the same types.
    proc Main()
    begin
        var b : bool;
        var s : string;
        var b2 : bool;
        s := "bad operand";
        b := 5 = b2;
    end;

end;